import "@fontsource-variable/inter";
import "./index.css";

import React from "react";
import ReactDOM from "react-dom/client";

import App from "./App.tsx";

ReactDOM.createRoot(document.getElementById("notetion-root")!).render(
    <React.StrictMode>
        <App />
    </React.StrictMode>
);
